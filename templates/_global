; vim: ft=colortemplate fdm=marker

#const transp_bg         = get(g:, '@prefix_transp_bg', 0)
#const supports_italic   = (&t_ZH != '' && &t_ZH != '[7m') || has('gui_running') || has('nvim')
#const italic            = get(g:, '@prefix_italics', 1) && s:supports_italic
#const italicize_strings = get(g:, '@prefix_italicize_strings', 1) && s:supports_italic
#const bold              = get(g:, '@prefix_bold', 1)
#const bold_italic       = s:italic + 2 * s:bold

Include: _linked_groups

